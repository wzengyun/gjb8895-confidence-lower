# GJB 8895 单侧置信下限计算器

这是一个纯静态网页项目，可部署到 GitHub Pages、Cloudflare Pages、Vercel、Netlify 或任意静态网站服务器。

## 文件

- `index.html`：完整单页应用，无需构建步骤。
- `vercel.json`：Vercel 静态部署配置。
- `netlify.toml`：Netlify 静态部署配置。
- `.nojekyll`：GitHub Pages 禁用 Jekyll 处理。

## 本地预览

直接双击 `index.html` 即可使用。
